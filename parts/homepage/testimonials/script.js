// Optional JS if we need to dynamically clone based on screen width
document.addEventListener('DOMContentLoaded', () => {
    // Current implementation uses CSS animation with PHP loop duplication.
    // If more robust infinite scroll is needed (e.g. based on width), we can add it here.
});
